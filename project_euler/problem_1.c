#include <stdio.h>

typedef struct node{
    int value;
    struct node * next;
} node_t;

node_t * head = NULL;
head = malloc(sizeof(node_t));
if(head == NULL)[

]





int main(){
    printf("Hello World!\n");
    int array[] = {1,2,3,4,5};
    print(array);

    return 0;
}
